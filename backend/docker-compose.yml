services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ski_app
      POSTGRES_PASSWORD: ski_app
      POSTGRES_DB: ski_app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
